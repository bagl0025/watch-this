<div class="shows">
  {{#each this}}
  <section class="reviews">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{reviews_text}}
    </div>
  </section>
  {{/each}}
</div>