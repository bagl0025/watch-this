<div class="reviews">
  {{#each this}}
  <section class="review m-3 p-3">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{reviews_text}}
    </div>
  </section>
  {{/each}}
</div>