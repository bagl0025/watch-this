<div class="shows">
  {{#each this}}
  <section class="show">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{shows_text}}
    </div>
  </section>
  {{/each}}
</div>